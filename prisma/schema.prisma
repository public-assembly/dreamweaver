generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Transaction {
  id        String @id
  address   String
  eventType String
  tags      Json
}

model RendererUpdated {
  id          String   @id
  targetPress String
  renderer    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PressInitialized {
  id          String   @id
  targetPress String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  sender      String
}

model LogicUpdated {
  id          String   @id
  targetPress String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  logic       String
}

model DataStored {
  id          String   @id
  targetPress String
  storeCaller String
  pointer     String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  tokenId     BigInt
}

model Create721Press {
  id              String  @id
  newPress        String
  initialOwner    String
  initialLogic    String
  creator         String
  initialRenderer String
  soulbound       Boolean
}
